# 해양수산부 대시보드 컴포넌트 명세서

## 목차
1. [컴포넌트 개요](#컴포넌트-개요)
2. [공통 컴포넌트](#공통-컴포넌트)
3. [레이아웃 컴포넌트](#레이아웃-컴포넌트)
4. [대시보드 컴포넌트](#대시보드-컴포넌트)
5. [게시물 컴포넌트](#게시물-컴포넌트)
6. [상태 컴포넌트](#상태-컴포넌트)

---

## 컴포넌트 개요

### 컴포넌트 계층 구조

```
App
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   ├── GlobalSearch
│   │   ├── NotificationBell
│   │   └── UserMenu
│   ├── Sidebar
│   │   ├── Navigation
│   │   └── QuickFilters
│   ├── MainContent
│   │   └── [Page Components]
│   └── Footer
│
├── Pages
│   ├── Dashboard
│   │   ├── StatCard (×4)
│   │   ├── DateRangePicker
│   │   ├── DonutChart
│   │   ├── BarChart
│   │   └── RecentPostsTable
│   │
│   ├── Posts
│   │   ├── FilterPanel
│   │   ├── SearchBar
│   │   ├── DataTable
│   │   └── Pagination
│   │
│   ├── Analysis
│   │   ├── TrendChart
│   │   ├── KeywordCloud
│   │   └── StatsSummary
│   │
│   └── Bookmarks
│       └── BookmarkList
│
└── Common
    ├── Button
    ├── Input
    ├── Select
    ├── Checkbox
    ├── DatePicker
    ├── Modal
    ├── Notification
    ├── LoadingSpinner
    ├── Skeleton
    └── ErrorBoundary
```

---

## 공통 컴포넌트

### 1. Button

**파일**: `src/components/common/Button/Button.tsx`

**Props**:
```typescript
interface ButtonProps {
  children: React.ReactNode;
  variant?: 'primary' | 'secondary' | 'outlined' | 'text' | 'danger';
  size?: 'sm' | 'md' | 'lg';
  disabled?: boolean;
  loading?: boolean;
  fullWidth?: boolean;
  startIcon?: React.ReactNode;
  endIcon?: React.ReactNode;
  onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void;
  type?: 'button' | 'submit' | 'reset';
  ariaLabel?: string;
  className?: string;
}
```

**사용 예시**:
```tsx
<Button
  variant="primary"
  size="md"
  startIcon={<SearchIcon />}
  onClick={handleSearch}
>
  검색
</Button>

<Button
  variant="outlined"
  loading={isLoading}
  disabled={!isValid}
>
  데이터 불러오기
</Button>
```

**스타일 변형**:

| Variant | 배경색 | 텍스트 색 | 테두리 | 용도 |
|---------|-------|---------|-------|-----|
| primary | #2196F3 | #FFFFFF | 없음 | 주요 액션 |
| secondary | #00BCD4 | #FFFFFF | 없음 | 보조 액션 |
| outlined | 투명 | #2196F3 | 1px #2196F3 | 중요도 낮음 |
| text | 투명 | #2196F3 | 없음 | 최소 강조 |
| danger | #F44336 | #FFFFFF | 없음 | 삭제 등 |

**접근성**:
```tsx
<button
  type={type}
  disabled={disabled || loading}
  aria-label={ariaLabel}
  aria-busy={loading}
  className={buttonClasses}
>
  {loading && <Spinner size="sm" />}
  {startIcon && <span className="icon-start">{startIcon}</span>}
  <span>{children}</span>
  {endIcon && <span className="icon-end">{endIcon}</span>}
</button>
```

---

### 2. Input

**Props**:
```typescript
interface InputProps {
  type?: 'text' | 'email' | 'password' | 'number' | 'search';
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  label?: string;
  helperText?: string;
  error?: boolean;
  errorMessage?: string;
  disabled?: boolean;
  required?: boolean;
  startAdornment?: React.ReactNode;
  endAdornment?: React.ReactNode;
  fullWidth?: boolean;
  name?: string;
  id?: string;
  autoComplete?: string;
}
```

**사용 예시**:
```tsx
<Input
  label="제목 검색"
  placeholder="검색어를 입력하세요"
  value={searchTerm}
  onChange={setSearchTerm}
  startAdornment={<SearchIcon />}
  endAdornment={
    searchTerm && (
      <IconButton onClick={() => setSearchTerm('')}>
        <ClearIcon />
      </IconButton>
    )
  }
/>

<Input
  label="이메일"
  type="email"
  value={email}
  onChange={setEmail}
  error={!!emailError}
  errorMessage={emailError}
  required
/>
```

**마크업 구조**:
```tsx
<div className="input-wrapper">
  {label && (
    <label htmlFor={id} className="input-label">
      {label}
      {required && <span className="required">*</span>}
    </label>
  )}
  <div className="input-container">
    {startAdornment && <div className="adornment-start">{startAdornment}</div>}
    <input
      id={id}
      type={type}
      value={value}
      onChange={(e) => onChange(e.target.value)}
      placeholder={placeholder}
      disabled={disabled}
      required={required}
      aria-invalid={error}
      aria-describedby={error ? `${id}-error` : helperText ? `${id}-helper` : undefined}
    />
    {endAdornment && <div className="adornment-end">{endAdornment}</div>}
  </div>
  {helperText && !error && (
    <div id={`${id}-helper`} className="helper-text">{helperText}</div>
  )}
  {error && errorMessage && (
    <div id={`${id}-error`} className="error-message" role="alert">
      {errorMessage}
    </div>
  )}
</div>
```

---

### 3. Select

**Props**:
```typescript
interface SelectOption {
  value: string | number;
  label: string;
  disabled?: boolean;
}

interface SelectProps {
  options: SelectOption[];
  value: string | number | string[] | number[];
  onChange: (value: string | number | string[] | number[]) => void;
  label?: string;
  placeholder?: string;
  multiple?: boolean;
  searchable?: boolean;
  disabled?: boolean;
  error?: boolean;
  errorMessage?: string;
  fullWidth?: boolean;
  clearable?: boolean;
}
```

**사용 예시**:
```tsx
<Select
  label="기관 구분"
  options={[
    { value: '', label: '전체' },
    { value: '본부', label: '본부 (1,234)' },
    { value: '지방청', label: '지방청 (3,456)' },
    { value: '항만공사', label: '항만공사 (789)' },
  ]}
  value={selectedOrg}
  onChange={setSelectedOrg}
  fullWidth
/>

<Select
  label="게시판"
  options={boards}
  value={selectedBoards}
  onChange={setSelectedBoards}
  multiple
  searchable
  clearable
/>
```

---

### 4. Checkbox

**Props**:
```typescript
interface CheckboxProps {
  checked: boolean;
  onChange: (checked: boolean) => void;
  label?: string;
  disabled?: boolean;
  indeterminate?: boolean;
  name?: string;
  value?: string;
  ariaLabel?: string;
}
```

**사용 예시**:
```tsx
<Checkbox
  label="공지사항 (5,432)"
  checked={filters.boards.includes('공지사항')}
  onChange={(checked) => toggleBoardFilter('공지사항', checked)}
/>

<Checkbox
  label="전체 선택"
  checked={allSelected}
  indeterminate={someSelected && !allSelected}
  onChange={handleSelectAll}
/>
```

---

### 5. DatePicker

**Props**:
```typescript
interface DatePickerProps {
  value: Date | null;
  onChange: (date: Date | null) => void;
  label?: string;
  minDate?: Date;
  maxDate?: Date;
  disabled?: boolean;
  error?: boolean;
  errorMessage?: string;
  placeholder?: string;
  format?: string; // 'YYYY-MM-DD'
}
```

**사용 예시**:
```tsx
<DatePicker
  label="시작일"
  value={startDate}
  onChange={setStartDate}
  maxDate={endDate || new Date()}
/>

<DatePicker
  label="종료일"
  value={endDate}
  onChange={setEndDate}
  minDate={startDate}
  maxDate={new Date()}
/>
```

---

### 6. Modal

**Props**:
```typescript
interface ModalProps {
  open: boolean;
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
  footer?: React.ReactNode;
  size?: 'sm' | 'md' | 'lg' | 'xl' | 'fullscreen';
  closeOnBackdropClick?: boolean;
  closeOnEscape?: boolean;
  showCloseButton?: boolean;
}
```

**사용 예시**:
```tsx
<Modal
  open={isOpen}
  onClose={handleClose}
  title="상세 정보"
  size="md"
  footer={
    <>
      <Button variant="outlined" onClick={handleClose}>
        취소
      </Button>
      <Button variant="primary" onClick={handleSave}>
        저장
      </Button>
    </>
  }
>
  <div>모달 콘텐츠</div>
</Modal>
```

**접근성 구현**:
```tsx
// 포커스 트랩
useEffect(() => {
  if (open) {
    const focusableElements = modalRef.current?.querySelectorAll(
      'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
    );
    const firstElement = focusableElements?.[0] as HTMLElement;
    const lastElement = focusableElements?.[focusableElements.length - 1] as HTMLElement;

    firstElement?.focus();

    const handleTab = (e: KeyboardEvent) => {
      if (e.key === 'Tab') {
        if (e.shiftKey) {
          if (document.activeElement === firstElement) {
            e.preventDefault();
            lastElement?.focus();
          }
        } else {
          if (document.activeElement === lastElement) {
            e.preventDefault();
            firstElement?.focus();
          }
        }
      }
    };

    document.addEventListener('keydown', handleTab);
    return () => document.removeEventListener('keydown', handleTab);
  }
}, [open]);

// ESC 키 처리
useEffect(() => {
  const handleEscape = (e: KeyboardEvent) => {
    if (e.key === 'Escape' && closeOnEscape) {
      onClose();
    }
  };

  if (open) {
    document.addEventListener('keydown', handleEscape);
    return () => document.removeEventListener('keydown', handleEscape);
  }
}, [open, closeOnEscape, onClose]);
```

---

### 7. Notification (Toast)

**Props**:
```typescript
interface NotificationProps {
  message: string;
  type?: 'success' | 'error' | 'warning' | 'info';
  duration?: number; // ms, 0 = 수동 닫기
  position?: 'top-left' | 'top-center' | 'top-right' | 'bottom-left' | 'bottom-center' | 'bottom-right';
  action?: {
    label: string;
    onClick: () => void;
  };
  onClose?: () => void;
}
```

**전역 알림 훅**:
```typescript
// useNotification.ts
const useNotification = () => {
  const addNotification = (options: NotificationProps) => {
    // 알림 큐에 추가
  };

  return { addNotification };
};

// 사용 예시
const { addNotification } = useNotification();

addNotification({
  message: '북마크에 추가되었습니다.',
  type: 'success',
  duration: 3000,
});

addNotification({
  message: '데이터를 불러오는 중 오류가 발생했습니다.',
  type: 'error',
  duration: 0, // 수동 닫기
  action: {
    label: '다시 시도',
    onClick: retryFetch,
  },
});
```

---

## 레이아웃 컴포넌트

### 1. Header

**파일**: `src/components/layout/Header/Header.tsx`

**Props**:
```typescript
interface HeaderProps {
  onMenuToggle?: () => void; // 모바일 메뉴 토글
}
```

**구조**:
```tsx
<header className="header" role="banner">
  <div className="header-container">
    {/* 좌측: 로고 + 메뉴 버튼 (모바일) */}
    <div className="header-left">
      <button
        className="menu-toggle md:hidden"
        onClick={onMenuToggle}
        aria-label="메뉴 열기"
      >
        <MenuIcon />
      </button>
      <Logo />
    </div>

    {/* 중앙: 검색 */}
    <div className="header-center hidden md:block">
      <GlobalSearch />
    </div>

    {/* 우측: 액션 */}
    <div className="header-right">
      <NotificationBell />
      <UserMenu />
    </div>
  </div>
</header>
```

---

### 2. Sidebar

**Props**:
```typescript
interface SidebarProps {
  isOpen: boolean; // 모바일에서 열림/닫힘
  onClose: () => void;
}
```

**구조**:
```tsx
<aside
  className={`sidebar ${isOpen ? 'open' : ''}`}
  role="navigation"
  aria-label="주 메뉴"
>
  <Navigation />

  <div className="sidebar-divider" />

  <QuickFilters />
</aside>
```

**Navigation 컴포넌트**:
```tsx
const Navigation = () => {
  const location = useLocation();

  const menuItems = [
    { path: '/', label: '대시보드', icon: <DashboardIcon /> },
    { path: '/posts', label: '전체 게시물', icon: <ListIcon /> },
    { path: '/analysis', label: '분석', icon: <ChartIcon /> },
    { path: '/bookmarks', label: '북마크', icon: <BookmarkIcon /> },
  ];

  return (
    <nav>
      <ul className="nav-list">
        {menuItems.map((item) => (
          <li key={item.path}>
            <NavLink
              to={item.path}
              className={({ isActive }) =>
                `nav-link ${isActive ? 'active' : ''}`
              }
              aria-current={location.pathname === item.path ? 'page' : undefined}
            >
              <span className="nav-icon">{item.icon}</span>
              <span className="nav-label">{item.label}</span>
            </NavLink>
          </li>
        ))}
      </ul>
    </nav>
  );
};
```

---

### 3. MainContent

**구조**:
```tsx
<main className="main-content" role="main" id="main-content">
  {children}
</main>
```

**스크롤 복원**:
```tsx
useEffect(() => {
  window.scrollTo(0, 0);
}, [location.pathname]);
```

---

## 대시보드 컴포넌트

### 1. StatCard

**Props**:
```typescript
interface StatCardProps {
  title: string;
  value: number | string;
  icon: React.ReactNode;
  trend?: {
    value: number; // 퍼센트
    direction: 'up' | 'down';
    label?: string; // "vs 지난주"
  };
  loading?: boolean;
  onClick?: () => void;
}
```

**구현**:
```tsx
const StatCard: React.FC<StatCardProps> = ({
  title,
  value,
  icon,
  trend,
  loading,
  onClick,
}) => {
  if (loading) {
    return <StatCardSkeleton />;
  }

  return (
    <div
      className={`stat-card ${onClick ? 'clickable' : ''}`}
      onClick={onClick}
      role={onClick ? 'button' : 'region'}
      aria-label={`통계: ${title}`}
      tabIndex={onClick ? 0 : undefined}
    >
      <div className="stat-icon">{icon}</div>

      <div className="stat-content">
        <div className="stat-title">{title}</div>
        <div className="stat-value">
          {typeof value === 'number' ? formatNumber(value) : value}
        </div>

        {trend && (
          <div className={`stat-trend trend-${trend.direction}`}>
            {trend.direction === 'up' ? '↗' : '↘'}
            <span>{Math.abs(trend.value)}%</span>
            {trend.label && <span className="trend-label">{trend.label}</span>}
          </div>
        )}
      </div>
    </div>
  );
};

// 숫자 포맷팅 유틸리티
const formatNumber = (num: number): string => {
  return num.toLocaleString('ko-KR');
};
```

**애니메이션**:
```css
.stat-card {
  animation: fadeInUp 0.3s ease-out;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 순차 애니메이션 */
.stat-card:nth-child(1) { animation-delay: 0s; }
.stat-card:nth-child(2) { animation-delay: 0.1s; }
.stat-card:nth-child(3) { animation-delay: 0.2s; }
.stat-card:nth-child(4) { animation-delay: 0.3s; }
```

---

### 2. DateRangePicker

**Props**:
```typescript
interface DateRangePickerProps {
  startDate: Date | null;
  endDate: Date | null;
  onChange: (range: { startDate: Date | null; endDate: Date | null }) => void;
  presets?: Array<{
    label: string;
    getValue: () => { startDate: Date; endDate: Date };
  }>;
}
```

**기본 프리셋**:
```typescript
const defaultPresets = [
  {
    label: '오늘',
    getValue: () => ({
      startDate: startOfDay(new Date()),
      endDate: endOfDay(new Date()),
    }),
  },
  {
    label: '최근 7일',
    getValue: () => ({
      startDate: subDays(new Date(), 7),
      endDate: new Date(),
    }),
  },
  {
    label: '최근 30일',
    getValue: () => ({
      startDate: subDays(new Date(), 30),
      endDate: new Date(),
    }),
  },
  {
    label: '이번 달',
    getValue: () => ({
      startDate: startOfMonth(new Date()),
      endDate: endOfMonth(new Date()),
    }),
  },
];
```

**사용 예시**:
```tsx
<DateRangePicker
  startDate={filters.startDate}
  endDate={filters.endDate}
  onChange={(range) => setFilters({ ...filters, ...range })}
/>
```

---

### 3. DonutChart

**Props**:
```typescript
interface ChartDataItem {
  name: string;
  value: number;
  color?: string;
}

interface DonutChartProps {
  data: ChartDataItem[];
  title?: string;
  showLegend?: boolean;
  onSegmentClick?: (item: ChartDataItem) => void;
  loading?: boolean;
}
```

**구현 (Recharts 사용)**:
```tsx
import { PieChart, Pie, Cell, ResponsiveContainer, Legend, Tooltip } from 'recharts';

const DonutChart: React.FC<DonutChartProps> = ({
  data,
  title,
  showLegend = true,
  onSegmentClick,
  loading,
}) => {
  if (loading) {
    return <ChartSkeleton />;
  }

  const COLORS = ['#2196F3', '#00BCD4', '#4CAF50', '#FF9800', '#F44336'];

  return (
    <div className="chart-container">
      {title && <h3 className="chart-title">{title}</h3>}

      <ResponsiveContainer width="100%" height={300}>
        <PieChart>
          <Pie
            data={data}
            cx="50%"
            cy="50%"
            innerRadius={60}
            outerRadius={80}
            paddingAngle={5}
            dataKey="value"
            onClick={onSegmentClick}
          >
            {data.map((entry, index) => (
              <Cell
                key={`cell-${index}`}
                fill={entry.color || COLORS[index % COLORS.length]}
                style={{ cursor: onSegmentClick ? 'pointer' : 'default' }}
              />
            ))}
          </Pie>
          <Tooltip
            formatter={(value: number) => formatNumber(value)}
            contentStyle={{ borderRadius: '8px' }}
          />
          {showLegend && <Legend />}
        </PieChart>
      </ResponsiveContainer>

      {/* 접근성: 데이터 테이블 */}
      <details className="chart-data-table">
        <summary>데이터 테이블 보기</summary>
        <table>
          <thead>
            <tr>
              <th>기관</th>
              <th>게시물 수</th>
              <th>비율</th>
            </tr>
          </thead>
          <tbody>
            {data.map((item, index) => {
              const total = data.reduce((sum, d) => sum + d.value, 0);
              const percentage = ((item.value / total) * 100).toFixed(1);
              return (
                <tr key={index}>
                  <td>{item.name}</td>
                  <td>{formatNumber(item.value)}</td>
                  <td>{percentage}%</td>
                </tr>
              );
            })}
          </tbody>
        </table>
      </details>
    </div>
  );
};
```

---

### 4. RecentPostsTable

**Props**:
```typescript
interface Post {
  id: string;
  orgCategory: string;
  orgName: string;
  board: string;
  title: string;
  createdAt: string;
  link: string;
  isBookmarked: boolean;
}

interface RecentPostsTableProps {
  posts: Post[];
  limit?: number;
  loading?: boolean;
  onRowClick?: (post: Post) => void;
  onBookmarkToggle?: (postId: string, isBookmarked: boolean) => void;
}
```

**구현**:
```tsx
const RecentPostsTable: React.FC<RecentPostsTableProps> = ({
  posts,
  limit = 10,
  loading,
  onRowClick,
  onBookmarkToggle,
}) => {
  if (loading) {
    return <TableSkeleton rows={limit} />;
  }

  const displayPosts = posts.slice(0, limit);

  return (
    <div className="recent-posts">
      <h2 className="section-title">최근 게시물</h2>

      <div className="table-wrapper">
        <table className="posts-table" role="table">
          <thead role="rowgroup">
            <tr role="row">
              <th role="columnheader" style={{ width: '48px' }}></th>
              <th role="columnheader">기관구분</th>
              <th role="columnheader">기관명</th>
              <th role="columnheader">게시판</th>
              <th role="columnheader">제목</th>
              <th role="columnheader">작성일</th>
              <th role="columnheader" style={{ width: '60px' }}></th>
            </tr>
          </thead>
          <tbody role="rowgroup">
            {displayPosts.map((post) => (
              <tr
                key={post.id}
                role="row"
                className="table-row"
                onClick={() => onRowClick?.(post)}
                style={{ cursor: onRowClick ? 'pointer' : 'default' }}
              >
                <td role="cell">
                  <button
                    className="bookmark-btn"
                    onClick={(e) => {
                      e.stopPropagation();
                      onBookmarkToggle?.(post.id, !post.isBookmarked);
                    }}
                    aria-label={post.isBookmarked ? '북마크 해제' : '북마크 추가'}
                  >
                    {post.isBookmarked ? '★' : '☆'}
                  </button>
                </td>
                <td role="cell">
                  <span className="badge badge-org">{post.orgCategory}</span>
                </td>
                <td role="cell">{post.orgName}</td>
                <td role="cell">
                  <span className={`badge badge-${getBoardColor(post.board)}`}>
                    {post.board}
                  </span>
                </td>
                <td role="cell" className="title-cell">
                  <a
                    href={post.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    onClick={(e) => e.stopPropagation()}
                  >
                    {post.title}
                  </a>
                </td>
                <td role="cell">{formatDate(post.createdAt)}</td>
                <td role="cell">
                  <a
                    href={post.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="link-icon"
                    aria-label="원문 보기"
                    onClick={(e) => e.stopPropagation()}
                  >
                    →
                  </a>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div className="table-footer">
        <Link to="/posts" className="view-all-link">
          전체 게시물 보기 →
        </Link>
      </div>
    </div>
  );
};

// 유틸리티 함수
const getBoardColor = (board: string): string => {
  const colorMap: Record<string, string> = {
    '공지사항': 'blue',
    '입찰공고': 'orange',
    '인사발령': 'green',
  };
  return colorMap[board] || 'gray';
};

const formatDate = (dateString: string): string => {
  const date = new Date(dateString);
  return date.toLocaleDateString('ko-KR', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
  });
};
```

---

## 게시물 컴포넌트

### 1. FilterPanel

**Props**:
```typescript
interface FilterOptions {
  orgCategories: string[];
  boards: string[];
  dateRange: {
    startDate: Date | null;
    endDate: Date | null;
  };
}

interface FilterPanelProps {
  filters: FilterOptions;
  onChange: (filters: FilterOptions) => void;
  onReset: () => void;
  onClose?: () => void;
  resultCount?: number;
  isOpen?: boolean; // 모바일에서 사용
}
```

**구현**:
```tsx
const FilterPanel: React.FC<FilterPanelProps> = ({
  filters,
  onChange,
  onReset,
  onClose,
  resultCount,
  isOpen = true,
}) => {
  const orgOptions = [
    { value: '본부', label: '본부', count: 1234 },
    { value: '지방청', label: '지방청', count: 3456 },
    { value: '항만공사', label: '항만공사', count: 789 },
    { value: '공단', label: '공단', count: 2345 },
    { value: '어업관리단', label: '어업관리단', count: 567 },
  ];

  const boardOptions = [
    { value: '공지사항', label: '공지사항', count: 5432 },
    { value: '입찰공고', label: '입찰공고', count: 2876 },
    { value: '인사발령', label: '인사발령', count: 1234 },
  ];

  return (
    <aside
      className={`filter-panel ${isOpen ? 'open' : ''}`}
      role="region"
      aria-label="필터"
    >
      <div className="filter-header">
        <h2>필터</h2>
        <div className="filter-actions">
          <Button variant="text" size="sm" onClick={onReset}>
            초기화
          </Button>
          {onClose && (
            <button
              className="close-btn"
              onClick={onClose}
              aria-label="필터 닫기"
            >
              ✕
            </button>
          )}
        </div>
      </div>

      <div className="filter-body">
        {/* 기관 구분 */}
        <fieldset className="filter-group">
          <legend className="filter-group-title">기관 구분</legend>
          {orgOptions.map((option) => (
            <Checkbox
              key={option.value}
              label={`${option.label} (${formatNumber(option.count)})`}
              checked={filters.orgCategories.includes(option.value)}
              onChange={(checked) => {
                const newCategories = checked
                  ? [...filters.orgCategories, option.value]
                  : filters.orgCategories.filter((c) => c !== option.value);
                onChange({ ...filters, orgCategories: newCategories });
              }}
            />
          ))}
        </fieldset>

        {/* 게시판 */}
        <fieldset className="filter-group">
          <legend className="filter-group-title">게시판</legend>
          {boardOptions.map((option) => (
            <Checkbox
              key={option.value}
              label={`${option.label} (${formatNumber(option.count)})`}
              checked={filters.boards.includes(option.value)}
              onChange={(checked) => {
                const newBoards = checked
                  ? [...filters.boards, option.value]
                  : filters.boards.filter((b) => b !== option.value);
                onChange({ ...filters, boards: newBoards });
              }}
            />
          ))}
        </fieldset>

        {/* 날짜 범위 */}
        <fieldset className="filter-group">
          <legend className="filter-group-title">작성일</legend>
          <DateRangePicker
            startDate={filters.dateRange.startDate}
            endDate={filters.dateRange.endDate}
            onChange={(range) =>
              onChange({ ...filters, dateRange: range })
            }
          />
        </fieldset>
      </div>

      <div className="filter-footer">
        {resultCount !== undefined && (
          <div className="result-count">
            검색 결과: {formatNumber(resultCount)}건
          </div>
        )}
        <Button variant="primary" fullWidth onClick={onClose}>
          필터 적용
        </Button>
      </div>
    </aside>
  );
};
```

---

### 2. DataTable

**Props**:
```typescript
interface Column<T> {
  key: keyof T | string;
  header: string;
  width?: string;
  sortable?: boolean;
  render?: (value: any, row: T) => React.ReactNode;
  align?: 'left' | 'center' | 'right';
}

interface DataTableProps<T> {
  data: T[];
  columns: Column<T>[];
  loading?: boolean;
  sortBy?: string;
  sortDirection?: 'asc' | 'desc';
  onSort?: (key: string) => void;
  onRowClick?: (row: T) => void;
  selectable?: boolean;
  selectedRows?: string[];
  onSelectionChange?: (selectedIds: string[]) => void;
  emptyMessage?: string;
}
```

**구현**:
```tsx
const DataTable = <T extends { id: string }>({
  data,
  columns,
  loading,
  sortBy,
  sortDirection,
  onSort,
  onRowClick,
  selectable,
  selectedRows = [],
  onSelectionChange,
  emptyMessage = '데이터가 없습니다.',
}: DataTableProps<T>) => {
  const allSelected = data.length > 0 && selectedRows.length === data.length;
  const someSelected = selectedRows.length > 0 && !allSelected;

  const handleSelectAll = (checked: boolean) => {
    if (checked) {
      onSelectionChange?.(data.map((row) => row.id));
    } else {
      onSelectionChange?.([]);
    }
  };

  const handleRowSelect = (id: string, checked: boolean) => {
    if (checked) {
      onSelectionChange?.([...selectedRows, id]);
    } else {
      onSelectionChange?.(selectedRows.filter((rowId) => rowId !== id));
    }
  };

  if (loading) {
    return <TableSkeleton rows={20} columns={columns.length} />;
  }

  if (data.length === 0) {
    return (
      <div className="table-empty">
        <div className="empty-icon">📭</div>
        <p>{emptyMessage}</p>
      </div>
    );
  }

  return (
    <div className="data-table-wrapper">
      <table className="data-table" role="table">
        <thead role="rowgroup">
          <tr role="row">
            {selectable && (
              <th role="columnheader" style={{ width: '48px' }}>
                <Checkbox
                  checked={allSelected}
                  indeterminate={someSelected}
                  onChange={handleSelectAll}
                  ariaLabel="전체 선택"
                />
              </th>
            )}
            {columns.map((column) => (
              <th
                key={column.key.toString()}
                role="columnheader"
                style={{
                  width: column.width,
                  textAlign: column.align || 'left',
                }}
                aria-sort={
                  sortBy === column.key
                    ? sortDirection === 'asc'
                      ? 'ascending'
                      : 'descending'
                    : 'none'
                }
              >
                {column.sortable && onSort ? (
                  <button
                    className="sort-button"
                    onClick={() => onSort(column.key.toString())}
                  >
                    {column.header}
                    <span className="sort-icon">
                      {sortBy === column.key ? (
                        sortDirection === 'asc' ? '↑' : '↓'
                      ) : (
                        '↕'
                      )}
                    </span>
                  </button>
                ) : (
                  column.header
                )}
              </th>
            ))}
          </tr>
        </thead>
        <tbody role="rowgroup">
          {data.map((row) => (
            <tr
              key={row.id}
              role="row"
              className={`table-row ${selectedRows.includes(row.id) ? 'selected' : ''}`}
              onClick={() => onRowClick?.(row)}
              style={{ cursor: onRowClick ? 'pointer' : 'default' }}
            >
              {selectable && (
                <td role="cell">
                  <Checkbox
                    checked={selectedRows.includes(row.id)}
                    onChange={(checked) => handleRowSelect(row.id, checked)}
                    ariaLabel={`행 ${row.id} 선택`}
                  />
                </td>
              )}
              {columns.map((column) => {
                const value = row[column.key as keyof T];
                return (
                  <td
                    key={column.key.toString()}
                    role="cell"
                    style={{ textAlign: column.align || 'left' }}
                  >
                    {column.render ? column.render(value, row) : String(value)}
                  </td>
                );
              })}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
};
```

**사용 예시**:
```tsx
const columns: Column<Post>[] = [
  {
    key: 'bookmark',
    header: '',
    width: '48px',
    render: (_, row) => (
      <button
        className="bookmark-btn"
        onClick={(e) => {
          e.stopPropagation();
          handleBookmarkToggle(row.id, !row.isBookmarked);
        }}
      >
        {row.isBookmarked ? '★' : '☆'}
      </button>
    ),
  },
  {
    key: 'orgCategory',
    header: '기관구분',
    width: '100px',
    sortable: true,
    render: (value) => <span className="badge badge-org">{value}</span>,
  },
  {
    key: 'orgName',
    header: '기관명',
    width: '120px',
    sortable: true,
  },
  {
    key: 'board',
    header: '게시판',
    width: '100px',
    sortable: true,
    render: (value) => (
      <span className={`badge badge-${getBoardColor(value)}`}>
        {value}
      </span>
    ),
  },
  {
    key: 'title',
    header: '제목',
    sortable: true,
    render: (value, row) => (
      <a
        href={row.link}
        target="_blank"
        rel="noopener noreferrer"
        onClick={(e) => e.stopPropagation()}
      >
        {value}
      </a>
    ),
  },
  {
    key: 'createdAt',
    header: '작성일',
    width: '120px',
    sortable: true,
    render: (value) => formatDate(value),
  },
  {
    key: 'link',
    header: '',
    width: '60px',
    align: 'center',
    render: (value) => (
      <a
        href={value}
        target="_blank"
        rel="noopener noreferrer"
        aria-label="원문 보기"
      >
        →
      </a>
    ),
  },
];

<DataTable
  data={posts}
  columns={columns}
  loading={isLoading}
  sortBy={sortBy}
  sortDirection={sortDirection}
  onSort={handleSort}
  onRowClick={handleRowClick}
/>
```

---

### 3. Pagination

**Props**:
```typescript
interface PaginationProps {
  currentPage: number;
  totalPages: number;
  onPageChange: (page: number) => void;
  siblingCount?: number; // 현재 페이지 양쪽에 표시할 페이지 수
  showFirstLast?: boolean;
  disabled?: boolean;
}
```

**구현**:
```tsx
const Pagination: React.FC<PaginationProps> = ({
  currentPage,
  totalPages,
  onPageChange,
  siblingCount = 1,
  showFirstLast = true,
  disabled = false,
}) => {
  const range = (start: number, end: number) => {
    return Array.from({ length: end - start + 1 }, (_, i) => start + i);
  };

  const paginationRange = useMemo(() => {
    const totalPageNumbers = siblingCount * 2 + 5; // 좌우 sibling + 현재 + 처음/끝 + 2개 점

    if (totalPages <= totalPageNumbers) {
      return range(1, totalPages);
    }

    const leftSiblingIndex = Math.max(currentPage - siblingCount, 1);
    const rightSiblingIndex = Math.min(currentPage + siblingCount, totalPages);

    const shouldShowLeftDots = leftSiblingIndex > 2;
    const shouldShowRightDots = rightSiblingIndex < totalPages - 1;

    if (!shouldShowLeftDots && shouldShowRightDots) {
      const leftRange = range(1, 3 + 2 * siblingCount);
      return [...leftRange, 'dots', totalPages];
    }

    if (shouldShowLeftDots && !shouldShowRightDots) {
      const rightRange = range(totalPages - (2 + 2 * siblingCount), totalPages);
      return [1, 'dots', ...rightRange];
    }

    const middleRange = range(leftSiblingIndex, rightSiblingIndex);
    return [1, 'dots', ...middleRange, 'dots', totalPages];
  }, [currentPage, totalPages, siblingCount]);

  return (
    <nav className="pagination" role="navigation" aria-label="페이지 네비게이션">
      <ul className="pagination-list">
        {/* 처음 페이지 */}
        {showFirstLast && (
          <li>
            <button
              className="pagination-btn"
              onClick={() => onPageChange(1)}
              disabled={disabled || currentPage === 1}
              aria-label="첫 페이지"
            >
              ≪
            </button>
          </li>
        )}

        {/* 이전 페이지 */}
        <li>
          <button
            className="pagination-btn"
            onClick={() => onPageChange(currentPage - 1)}
            disabled={disabled || currentPage === 1}
            aria-label="이전 페이지"
          >
            ◀
          </button>
        </li>

        {/* 페이지 번호 */}
        {paginationRange.map((pageNumber, index) => {
          if (pageNumber === 'dots') {
            return (
              <li key={`dots-${index}`}>
                <span className="pagination-dots">...</span>
              </li>
            );
          }

          return (
            <li key={pageNumber}>
              <button
                className={`pagination-btn ${pageNumber === currentPage ? 'active' : ''}`}
                onClick={() => onPageChange(Number(pageNumber))}
                disabled={disabled}
                aria-label={`페이지 ${pageNumber}`}
                aria-current={pageNumber === currentPage ? 'page' : undefined}
              >
                {pageNumber}
              </button>
            </li>
          );
        })}

        {/* 다음 페이지 */}
        <li>
          <button
            className="pagination-btn"
            onClick={() => onPageChange(currentPage + 1)}
            disabled={disabled || currentPage === totalPages}
            aria-label="다음 페이지"
          >
            ▶
          </button>
        </li>

        {/* 마지막 페이지 */}
        {showFirstLast && (
          <li>
            <button
              className="pagination-btn"
              onClick={() => onPageChange(totalPages)}
              disabled={disabled || currentPage === totalPages}
              aria-label="마지막 페이지"
            >
              ≫
            </button>
          </li>
        )}
      </ul>
    </nav>
  );
};
```

---

## 상태 컴포넌트

### 1. LoadingSpinner

**Props**:
```typescript
interface LoadingSpinnerProps {
  size?: 'sm' | 'md' | 'lg';
  color?: string;
  fullScreen?: boolean;
  message?: string;
}
```

**구현**:
```tsx
const LoadingSpinner: React.FC<LoadingSpinnerProps> = ({
  size = 'md',
  color = '#2196F3',
  fullScreen = false,
  message,
}) => {
  const sizeMap = {
    sm: '24px',
    md: '40px',
    lg: '64px',
  };

  const spinner = (
    <div className={`spinner spinner-${size}`}>
      <svg
        width={sizeMap[size]}
        height={sizeMap[size]}
        viewBox="0 0 50 50"
        xmlns="http://www.w3.org/2000/svg"
      >
        <circle
          cx="25"
          cy="25"
          r="20"
          fill="none"
          stroke={color}
          strokeWidth="4"
          strokeLinecap="round"
          strokeDasharray="80, 200"
          strokeDashoffset="0"
        >
          <animateTransform
            attributeName="transform"
            type="rotate"
            from="0 25 25"
            to="360 25 25"
            dur="1s"
            repeatCount="indefinite"
          />
        </circle>
      </svg>
      {message && <p className="spinner-message">{message}</p>}
    </div>
  );

  if (fullScreen) {
    return (
      <div className="spinner-fullscreen">
        {spinner}
      </div>
    );
  }

  return spinner;
};
```

---

### 2. Skeleton

**Props**:
```typescript
interface SkeletonProps {
  variant?: 'text' | 'rect' | 'circle';
  width?: string | number;
  height?: string | number;
  animation?: 'pulse' | 'wave' | 'none';
}
```

**특수 스켈레톤 컴포넌트**:

```tsx
// StatCardSkeleton
const StatCardSkeleton: React.FC = () => (
  <div className="stat-card skeleton">
    <Skeleton variant="circle" width={48} height={48} />
    <div className="stat-content">
      <Skeleton variant="text" width="60%" height={16} />
      <Skeleton variant="text" width="80%" height={32} />
      <Skeleton variant="text" width="50%" height={14} />
    </div>
  </div>
);

// TableSkeleton
const TableSkeleton: React.FC<{ rows?: number; columns?: number }> = ({
  rows = 10,
  columns = 7,
}) => (
  <div className="table-skeleton">
    <table>
      <thead>
        <tr>
          {Array.from({ length: columns }).map((_, i) => (
            <th key={i}>
              <Skeleton variant="text" width="80%" height={16} />
            </th>
          ))}
        </tr>
      </thead>
      <tbody>
        {Array.from({ length: rows }).map((_, rowIndex) => (
          <tr key={rowIndex}>
            {Array.from({ length: columns }).map((_, colIndex) => (
              <td key={colIndex}>
                <Skeleton variant="text" width="90%" height={16} />
              </td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  </div>
);

// ChartSkeleton
const ChartSkeleton: React.FC = () => (
  <div className="chart-skeleton">
    <Skeleton variant="text" width="40%" height={24} />
    <Skeleton variant="rect" width="100%" height={300} />
  </div>
);
```

---

### 3. ErrorBoundary

**구현**:
```tsx
interface ErrorBoundaryProps {
  children: React.ReactNode;
  fallback?: React.ReactNode;
  onError?: (error: Error, errorInfo: React.ErrorInfo) => void;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error?: Error;
}

class ErrorBoundary extends React.Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('ErrorBoundary caught an error:', error, errorInfo);
    this.props.onError?.(error, errorInfo);
  }

  handleReset = () => {
    this.setState({ hasError: false, error: undefined });
  };

  render() {
    if (this.state.hasError) {
      if (this.props.fallback) {
        return this.props.fallback;
      }

      return (
        <div className="error-boundary">
          <div className="error-icon">⚠️</div>
          <h2>문제가 발생했습니다</h2>
          <p>페이지를 표시하는 중 오류가 발생했습니다.</p>
          {this.state.error && (
            <details className="error-details">
              <summary>오류 상세 정보</summary>
              <pre>{this.state.error.message}</pre>
            </details>
          )}
          <div className="error-actions">
            <Button variant="primary" onClick={this.handleReset}>
              다시 시도
            </Button>
            <Button variant="outlined" onClick={() => window.location.href = '/'}>
              홈으로 이동
            </Button>
          </div>
        </div>
      );
    }

    return this.props.children;
  }
}
```

**사용 예시**:
```tsx
<ErrorBoundary
  onError={(error, errorInfo) => {
    // 에러 로깅 서비스로 전송 (예: Sentry)
    Sentry.captureException(error, { extra: errorInfo });
  }}
>
  <Dashboard />
</ErrorBoundary>
```

---

### 4. EmptyState

**Props**:
```typescript
interface EmptyStateProps {
  icon?: React.ReactNode;
  title: string;
  description?: string;
  action?: {
    label: string;
    onClick: () => void;
  };
}
```

**구현**:
```tsx
const EmptyState: React.FC<EmptyStateProps> = ({
  icon = '📭',
  title,
  description,
  action,
}) => (
  <div className="empty-state">
    <div className="empty-icon">{icon}</div>
    <h3 className="empty-title">{title}</h3>
    {description && <p className="empty-description">{description}</p>}
    {action && (
      <Button variant="primary" onClick={action.onClick}>
        {action.label}
      </Button>
    )}
  </div>
);

// 사용 예시
<EmptyState
  icon="🔍"
  title="검색 결과가 없습니다"
  description="다른 검색어를 시도하거나 필터를 조정해보세요."
  action={{
    label: '필터 초기화',
    onClick: resetFilters,
  }}
/>
```

---

## 컴포넌트 스타일 가이드

### CSS 모듈 예시

**Button.module.css**:
```css
.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 8px 16px;
  font-size: 14px;
  font-weight: 600;
  border-radius: 4px;
  border: none;
  cursor: pointer;
  transition: all 0.2s ease;
}

.button:focus-visible {
  outline: 2px solid #1976D2;
  outline-offset: 2px;
}

.button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* Variants */
.primary {
  background-color: #2196F3;
  color: #FFFFFF;
}

.primary:hover:not(:disabled) {
  background-color: #1976D2;
}

.secondary {
  background-color: #00BCD4;
  color: #FFFFFF;
}

.outlined {
  background-color: transparent;
  border: 1px solid #2196F3;
  color: #2196F3;
}

.text {
  background-color: transparent;
  color: #2196F3;
}

/* Sizes */
.sm {
  padding: 4px 12px;
  font-size: 12px;
}

.md {
  padding: 8px 16px;
  font-size: 14px;
}

.lg {
  padding: 12px 24px;
  font-size: 16px;
}

.fullWidth {
  width: 100%;
}
```

---

## 테스트 예시

**Button.test.tsx**:
```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import { Button } from './Button';

describe('Button', () => {
  it('renders with children', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByRole('button', { name: 'Click me' })).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const handleClick = jest.fn();
    render(<Button onClick={handleClick}>Click me</Button>);

    fireEvent.click(screen.getByRole('button'));
    expect(handleClick).toHaveBeenCalledTimes(1);
  });

  it('disables button when disabled prop is true', () => {
    render(<Button disabled>Click me</Button>);
    expect(screen.getByRole('button')).toBeDisabled();
  });

  it('shows loading spinner when loading', () => {
    render(<Button loading>Click me</Button>);
    expect(screen.getByRole('button')).toHaveAttribute('aria-busy', 'true');
  });

  it('renders with start and end icons', () => {
    render(
      <Button
        startIcon={<span data-testid="start-icon">→</span>}
        endIcon={<span data-testid="end-icon">←</span>}
      >
        Click me
      </Button>
    );

    expect(screen.getByTestId('start-icon')).toBeInTheDocument();
    expect(screen.getByTestId('end-icon')).toBeInTheDocument();
  });
});
```

---

## 종합 체크리스트

### 컴포넌트 개발 시 확인사항

- [ ] TypeScript Props 인터페이스 정의
- [ ] 기본값(defaultProps) 설정
- [ ] 로딩 상태 처리
- [ ] 에러 상태 처리
- [ ] 빈 상태 처리
- [ ] 접근성 속성 (ARIA)
- [ ] 키보드 네비게이션
- [ ] 반응형 스타일
- [ ] 다크 모드 지원
- [ ] 단위 테스트 작성
- [ ] Storybook 스토리 작성 (선택)
- [ ] JSDoc 주석 (복잡한 로직)

### 성능 최적화

- [ ] React.memo로 불필요한 리렌더 방지
- [ ] useMemo로 계산 비용이 큰 값 메모이제이션
- [ ] useCallback으로 함수 메모이제이션
- [ ] 가상 스크롤링 (대용량 리스트)
- [ ] 이미지 lazy loading
- [ ] 코드 스플리팅 (페이지 단위)

---

이 컴포넌트 명세서는 개발팀이 일관된 품질의 UI 컴포넌트를 만들 수 있도록 상세한 가이드를 제공합니다.
